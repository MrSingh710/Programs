# while with 'else' also exists

l = [1, False, "Jassi", 5.5, 3]

for i in l:
    print(i)

else:               # else is executed when all the loops are executed or the condition gets false
    print("Done")   # else is not executed if break statement terminates the loop